# مذكّرة تقنية لفريق تقنية المعلومات — مشكلة حفظ المرفقات كـ PDF دون استخراج البيانات

**الموضوع:** لماذا تُحفظ التقارير المرفوعة على شكل PDF ولا تظهر بياناتها كمحتوى تفاعلي، وما المطلوب لتشغيل الاستخراج التلقائي على الدومين والاستضافة الخاصة بنا.

**الموجز التنفيذي:** الموقع بأكمله يعمل على أي استضافة ثابتة. لكن خاصية **تحويل ملف PDF المرفوع إلى محتوى تفاعلي (استخراج البيانات)** تحتاج **نقطة نهاية خلفية واحدة (Backend endpoint)** تستدعي واجهة Claude API بمفتاح محفوظ في الخادم. طالما لم تُنشر هذه النقطة الخلفية، سيستمر النظام في حفظ الملف كـ PDF فقط بحالة «يحتاج معالجة» — وهذا سلوك مقصود وليس خطأً برمجيًا.

---

## 1. جذر المشكلة

رفع التقرير في صفحة الإدارة يقوم بعمليتين **منفصلتين**:

| العملية | تحدث دائمًا؟ | تعتمد على |
|---|---|---|
| حفظ ملف PDF الأصلي | ✅ نعم | لا شيء |
| **استخراج البيانات** (قراءة نص PDF وتحويله إلى JSON منظّم يغذّي البوابات والأرشيف) | ❌ فقط عند توفر خدمة استخراج | مفتاح Claude API + نقطة نهاية خلفية |

النظام مصمَّم ليجرّب ثلاثة مسارات للاستخراج بالترتيب:

1. **بيئة Claude Design** (`window.claude.complete`) — تعمل فقط داخل معاينة claude.ai/design. **لن تتوفر على استضافتنا.**
2. **نقطة النهاية الخلفية `POST /api/extract`** — تستدعي Claude API من الخادم، والمفتاح يبقى في الخادم ولا يصل للمتصفح. **هذا هو الحل المطلوب للإنتاج.**
3. **الاتصال المباشر من المتصفح** بمفتاح يحفظه المستخدم محليًا — بديل مؤقت للاستضافة الثابتة فقط، **غير موصى به للإنتاج** (يتطلب أن يملك كل مستخدم مفتاحًا، ويعرّض إدارة المفتاح لجانب العميل).

**الحالة الحالية:** الموقع منشور كملفات ثابتة بلا خادم، فالمسار (1) و(2) غير متوفرين، والمسار (3) يتطلب حفظ مفتاح يدويًا. لذلك يُحفظ كل رفع كـ PDF فقط.

---

## 2. البنية التقنية للاستخراج

```
[صفحة الإدارة في المتصفح]
   │  1) قراءة نص ملف PDF داخل المتصفح (مكتبة pdf-parse)
   │     ⟵ يُستخرج النص فقط، وليس الملف الثنائي
   │
   │  2) POST /api/extract  { type: "wgs" | "demand", text: "<نص التقرير>" }
   ▼
[نقطة النهاية الخلفية /api/extract  ← يجب أن ننشرها]
   │  3) تستدعي Claude API (المفتاح من متغير بيئة في الخادم)
   │     تحوّل النص إلى JSON بالمخطط المطلوب لكل نوع تقرير
   ▼
   ⟶ ترجع { data: <JSON منظّم> }
       يُحفظ ضمن حقل data للتقرير ⟶ تظهر البيانات في البوابات والأرشيف
```

**نقطة مهمة:** ملف PDF لا يُرسل إلى الخادم؛ يُقرأ نصه في المتصفح أولًا، ويُرسل **النص فقط**. هذا يقلّل الحمل ويتجنّب رفع ملفات كبيرة.

---

## 3. المطلوب من IT لتشغيل الاستخراج على استضافتنا

### الخطوة الأساسية: نشر نقطة النهاية الخلفية `/api/extract`

الكود جاهز في المستودع: `api/extract.js`. هو دالة Node.js بصيغة `(req, res)` قياسية. أمامكم خياران:

#### الخيار (أ) — تشغيله كما هو على منصة تدعم الدوال الخادمية
إن كانت استضافتنا تدعم Node serverless functions (مثل Vercel أو بيئة مماثلة داخلية): ارفعوا المستودع، واضبطوا متغيّر البيئة، وسيعمل تلقائيًا.

#### الخيار (ب) — تشغيله كخدمة Node خلف خادم الويب (nginx / IIS / Apache)
لُفّوا الدالة بخادم Express بسيط (بضعة أسطر) أو أعيدوا كتابة المنطق نفسه بلغة الباك-إند لدينا. **عقد الواجهة الذي يجب الالتزام به بدقة:**

| العنصر | القيمة |
|---|---|
| المسار | `POST /api/extract` (نسبي — يجب أن يكون على نفس أصل الموقع، أو اضبطوا CORS) |
| الطلب (Request body) | `{ "type": "wgs" \| "demand", "text": "<string>" }` |
| الاستجابة عند النجاح | `200` + `{ "data": <object> }` — الكائن يتبع مخطط JSON لنوع التقرير |
| أخطاء متوقعة | `400` طلب غير صحيح · `422` رفض النموذج / نص كبير جدًا / JSON غير صالح · `429` تجاوز الحد · `502` خطأ من Claude API · `503` المفتاح غير مضبوط |
| المصادقة | يجب أن نضيفها (انظر §4) — النسخة الحالية بلا مصادقة |

مخطّطا JSON للنوعين (`SCHEMA_WGS` و`SCHEMA_DEMAND`) موجودان داخل `api/extract.js` — لا تغيّروهما حتى تبقى البيانات متوافقة مع صفحات العرض.

### متغيّرات البيئة المطلوبة على الخادم

| المتغيّر | إلزامي | الوصف |
|---|---|---|
| `ANTHROPIC_API_KEY` | ✅ | مفتاح Claude API من console.anthropic.com — يبقى في الخادم فقط ولا يصل للمتصفح إطلاقًا |
| `EXTRACT_MODEL` | اختياري | لتغيير النموذج (الافتراضي `claude-opus-4-8`) |

### ملفات الموقع الثابتة

بقية الموقع (HTML/CSS/JS) ملفات ثابتة عادية: قدّموها عبر HTTP(S) من جذر المستودع. ملاحظة: يستخدم الموقع ES Modules، لذا **يجب تقديمه عبر HTTP وليس فتح الملفات مباشرة من القرص** (`file://` لن يعمل).

---

## 4. متطلبات تجهيز الإنتاج (مهمة)

- [ ] **حماية `/api/extract` بمصادقة** — النسخة الحالية يستطيع استدعاءها أي شخص يصل للرابط (تستهلك رصيد API). اربطوها بمصادقة النظام أو بروكسي.
- [ ] **حماية صفحة الإدارة `admin-upload.html` بمصادقة (SSO)** — حاليًا مفتوحة (نموذج أولي).
- [ ] **الاتصال الخارجي:** الخادم يحتاج وصولًا صادرًا إلى `api.anthropic.com`. تأكدوا أن جدار الحماية يسمح به.
- [ ] **CORS:** إن كانت نقطة النهاية على أصل مختلف عن الموقع، اضبطوا ترويسات CORS؛ الأبسط أن تكون على نفس الدومين.
- [ ] **الخطوط والمكتبات:** الموقع يجلب خطوط Google ومكتبة pdf-parse من CDN. إن كانت الشبكة مقيّدة، استضيفوها محليًا.
- [ ] **التخزين المشترك:** حاليًا التقارير المرفوعة تُحفظ في `localStorage` بمتصفح كل مستخدم (غير مشتركة). للإنتاج متعدد المستخدمين، استبدلوها بقاعدة بيانات — التفاصيل في وثيقة `HANDOVER.md` القسم 4 (نفس شكل JSON، ثلاث نقاط نهاية فقط).

---

## 5. ماذا لو لم نُشغّل الاستخراج الآلي؟

النظام لا يفقد أي رفع: يبقى ملف PDF محفوظًا ومتاحًا للتحميل والعرض في الأرشيف، بحالة «PDF فقط — يحتاج معالجة»، ويمكن معالجته لاحقًا بزر «معالجة المحتوى» فور تشغيل نقطة النهاية. أي أن غياب الاستخراج لا يعطّل الموقع — فقط لا تتحول التقارير المرفوعة تلقائيًا إلى الشكل التفاعلي.

**بديل بدون AI إطلاقًا:** يمكن أيضًا إدخال بيانات التقارير مباشرة في مصدر البيانات الموحّد `reports-data.js` (أو قاعدة البيانات عند ربطها) بدل الاستخراج من PDF — عندها تظهر كمحتوى تفاعلي دون الحاجة لأي مفتاح API. الاستخراج الآلي مجرد وسيلة راحة لتحويل ملفات PDF بسرعة.

---

## 6. خلاصة ما نطلبه من IT

1. نشر ملفات الموقع الثابتة على الدومين الخاص بنا عبر HTTPS.
2. نشر نقطة النهاية `/api/extract` (كما هي كدالة Node، أو ضمن الباك-إند لدينا بنفس العقد في §3).
3. ضبط متغيّر البيئة `ANTHROPIC_API_KEY` على الخادم، والسماح بالوصول الصادر إلى `api.anthropic.com`.
4. إضافة المصادقة على صفحة الإدارة وعلى نقطة النهاية.
5. (للإنتاج الكامل) استبدال تخزين `localStorage` بقاعدة بيانات مشتركة — انظر `HANDOVER.md`.

بعد الخطوات 1–3 سيعمل الاستخراج التلقائي: كل تقرير يُرفع يتحوّل مباشرة إلى محتوى تفاعلي في البوابات والأرشيف، ولن يبقى «PDF فقط».

> المرجع الكامل للبنية والنشر ونموذج البيانات في ملف `HANDOVER.md` بالمستودع.
